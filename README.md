# Dominated Novelty Search Animation

## Install

To install the package:
```
uv venv
source .venv/bin/activate
uv pip install =e .
```

## Run

To create the animations:
```
uv run manim -qm main.py GeneticAlgorithm MapElites DominatedNoveltySearch NoveltySearch
```
